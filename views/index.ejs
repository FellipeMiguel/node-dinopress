<%- include('partials/header') %>
<%- include('./partials/homeNavbar') %>

<div class="container">
    <hr />
    <div class="jumbotron">
        <h1 class="display-4">Bem-vindo!</h1>
        <p class="lead">Dino's Press</p>
        <hr class="my-4">
        <p>Esté um blog para fins de estudos</p>
        <a href="" class="btn btn-success btn-lg" role="button">Leia mais</a>
    </div>
    <hr />
    <% articles.forEach(article => { %>
        <div class="card">
            <div class="card-header">
                <h2><%= article.title %></h2>
            </div>
            <div class="card-body">
                <a href="/<%= article.slug %>" class="btn btn-success">Ler artigo</a>
            </div>
        </div>
        <br />
    <% }) %>
</div>

<%- include('partials/footer') %>